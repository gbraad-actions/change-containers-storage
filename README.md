Change containers storage action
================================

Move containers storage to /mnt on runner

```yaml
- name: Change containers storage
  uses: gbraad-actions/change-containers-storage@v1
```
